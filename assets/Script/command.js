/*
客户端处理服务端的数据包接口，协议由程序商量
0号协议为空协议不要使用
*/
cc.Class({

    oncommand:function(idx,len,data){
        switch (idx) {
            case 0:               
                break;
        
            default:
                break;
        }
    },
})